const visitDate = Object.freeze({
  OUT_OF_RANGE_DAY: '[ERROR] 유효하지 않은 날짜입니다. 다시 입력해 주세요.',
});

const menu = Object.freeze({
  NO_MENU: '[ERROR] 유효하지 않은 주문입니다. 다시 입력해 주세요.',
});

const ERROR = Object.freeze({
  menu,
  visitDate,
});

export default ERROR;
