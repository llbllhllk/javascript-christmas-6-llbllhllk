const visitDate = Object.freeze({
  outOfRangeDay: '[ERROR] 유효하지 않은 날짜입니다. 다시 입력해 주세요.',
});

const menu = Object.freeze({
  invalidOrder: '[ERROR] 유효하지 않은 주문입니다. 다시 입력해 주세요.',
});

const ERROR = Object.freeze({
  menu,
  visitDate,
});

export default ERROR;
